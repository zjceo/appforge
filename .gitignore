# ============================================
# AppForge - .gitignore
# ============================================
# Este archivo excluye archivos y directorios que se generan
# automáticamente cuando ejecutas ./install.sh en tu VPS.
# NO es necesario que estos archivos estén en el repositorio.
# ============================================

# ============================================
# DIRECTORIOS GENERADOS AUTOMÁTICAMENTE
# ============================================
# Estos directorios se crean automáticamente por install.sh
# cuando instalas aplicaciones en tu VPS

# Aplicaciones instaladas (contenido específico de cada VPS)
# Cada instancia tiene su configuración única con credenciales
apps/

# Backups de aplicaciones (generados por scripts/backup.sh)
backups/

# Certificados SSL generados localmente
certs/

# ============================================
# ARCHIVOS DE CREDENCIALES Y CONFIGURACIÓN
# ============================================
# ⚠️ IMPORTANTE: Estos archivos contienen credenciales sensibles
# que NO deben subirse al repositorio

# Credenciales guardadas de todas las aplicaciones instaladas
credentials.txt

# Archivos de entorno con contraseñas y secretos
# Generados automáticamente en apps/*/.env
apps/*/.env
apps/*/.env.local
apps/*/.env.*.local

# Archivos de entorno de ejemplo (opcional, descomentar si no quieres incluirlos)
# apps/*/.env.example

# ============================================
# CONFIGURACIÓN DEL PROXY (TRAEFIK)
# ============================================
# Configuración y certificados del proxy inverso

# Certificados SSL de Let's Encrypt (acme.json)
proxy/traefik/acme.json

# Configuración del proxy generada automáticamente
# (se crea al ejecutar setup_proxy por primera vez)
proxy/traefik.yml
proxy/traefik/*.yml
proxy/traefik/*.json

# Mantener la estructura del directorio proxy si es necesario
!proxy/.gitkeep

# ============================================
# LOGS Y ARCHIVOS TEMPORALES
# ============================================

# Logs de aplicaciones
*.log
logs/
*.log.*

# Archivos temporales
*.tmp
*.temp
*.swp
*.swo
*~

# Archivos de backup temporales
*.bak
*.backup

# ============================================
# ARCHIVOS DEL SISTEMA OPERATIVO
# ============================================

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# Linux
*~
.directory
.Trash-*
.nfs*

# ============================================
# ARCHIVOS DE EDITORES E IDEs
# ============================================

# Vim
*.swp
*.swo
*.un~
Session.vim
.netrwhist

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*

# VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains IDEs
.idea/
*.iml
*.iws
*.ipr
out/

# Sublime Text
*.sublime-workspace
*.sublime-project

# ============================================
# DOCKER Y CONTENEDORES
# ============================================

# Volúmenes de Docker (si se exportan)
docker-volumes/
.docker/

# Compose override files locales
docker-compose.override.yml
docker-compose.local.yml

# ============================================
# VARIABLES DE ENTORNO Y SECRETOS
# ============================================

# Archivos de entorno en la raíz del proyecto
.env
.env.local
.env.*.local
.env.production

# Solo mantener .env.example como referencia
!.env.example

# ============================================
# ARCHIVOS DE INSTALACIÓN TEMPORALES
# ============================================

# Scripts temporales generados durante instalación
get-docker.sh
install-*.sh
setup-*.sh

# ============================================
# NOTAS
# ============================================
# - Los directorios apps/, backups/, certs/ y proxy/traefik/ 
#   se crean automáticamente cuando ejecutas ./install.sh
# - El archivo credentials.txt se genera al instalar tu primera app
# - NO es necesario crear estos directorios manualmente
# - Si quieres mantener la estructura vacía en el repo, puedes agregar
#   archivos .gitkeep (pero NO es necesario para el funcionamiento)

