# RabbitMQ Configuration
# Generado automáticamente por AppForge

# Dominio (Management UI)
DOMAIN=mq.example.com
APP_NAME=rabbitmq_1
SERVICE_NUMBER=1

# Base de datos (RabbitMQ no usa DB tradicional)
DB_NAME=rabbitmq_1
DB_USER=admin
DB_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# Credenciales de administrador
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# API Key (no aplica en RabbitMQ)
API_KEY=CHANGE_ME_RANDOM_KEY

# Puerto AMQP (expuesto externamente)
DB_PORT=5672

# Virtual Host
RABBITMQ_DEFAULT_VHOST=/

# Límites de recursos
RABBITMQ_VM_MEMORY_HIGH_WATERMARK=512MiB
RABBITMQ_DISK_FREE_LIMIT=2GB

# Notas de instalación:
# 1. Management UI: https://mq.example.com
# 2. Usuario: el configurado en DB_USER
# 3. Password: el configurado en DB_PASSWORD
# 4. AMQP URL: amqp://admin:PASSWORD@IP:5672/
# 5. Management API: https://mq.example.com/api/

# Connection string para aplicaciones:
# amqp://admin:PASSWORD@rabbitmq_1:5672/
# amqp://admin:PASSWORD@rabbitmq_1:5672/vhost_name

# Crear virtual host:
# docker exec rabbitmq_1 rabbitmqctl add_vhost my_vhost
# docker exec rabbitmq_1 rabbitmqctl set_permissions -p my_vhost admin ".*" ".*" ".*"

# Crear usuario adicional:
# docker exec rabbitmq_1 rabbitmqctl add_user myuser mypassword
# docker exec rabbitmq_1 rabbitmqctl set_user_tags myuser administrator
# docker exec rabbitmq_1 rabbitmqctl set_permissions -p / myuser ".*" ".*" ".*"

# Comandos útiles:
# rabbitmqctl list_users                    # Listar usuarios
# rabbitmqctl list_vhosts                   # Listar vhosts
# rabbitmqctl list_queues                   # Listar queues
# rabbitmqctl list_exchanges                # Listar exchanges
# rabbitmqctl list_bindings                 # Listar bindings

# Monitoreo:
# rabbitmqctl status                        # Estado del servidor
# rabbitmqctl cluster_status                # Estado del cluster
# rabbitmq-diagnostics ping                 # Verificar que funciona