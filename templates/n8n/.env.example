# N8N Configuration
# Generado automáticamente por AppForge

# Dominio
DOMAIN=n8n.example.com
APP_NAME=n8n_1
SERVICE_NUMBER=1

# Base de datos PostgreSQL
DB_NAME=n8n_db_1
DB_USER=n8n
DB_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# Credenciales de administrador
ADMIN_EMAIL=n8n@example.com
ADMIN_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# API Key / Secret
API_KEY=CHANGE_ME_RANDOM_KEY

# Configuración adicional
TIMEZONE=America/Lima

# SMTP (opcional - para notificaciones)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=true
SMTP_FROM=n8n@example.com

# Notas de instalación:
# 1. Este archivo se genera automáticamente durante la instalación
# 2. Las contraseñas se generan de forma aleatoria
# 3. Puedes cambiar el timezone según tu ubicación
# 4. Configura SMTP si deseas recibir notificaciones por email