# MinIO Configuration
# Generado automáticamente por AppForge

# Dominios
DOMAIN=s3.example.com
CONSOLE_DOMAIN=console.s3.example.com
APP_NAME=minio_1
SERVICE_NUMBER=1

# Base de datos (MinIO no usa DB tradicional)
DB_NAME=minio_1
DB_USER=minio
DB_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# Credenciales de administrador (Root User)
ADMIN_EMAIL=admin
ADMIN_PASSWORD=CHANGE_ME_RANDOM_PASSWORD_MIN_8_CHARS

# API Key (se usa ADMIN_EMAIL como access key)
API_KEY=CHANGE_ME_RANDOM_KEY

# Configuración
MINIO_REGION_NAME=us-east-1

# Buckets por defecto (opcional - crear manualmente)
DEFAULT_BUCKETS=typebot,chatwoot,nocodb,n8n

# Notas de instalación:
# 1. API S3: https://s3.example.com
# 2. Console Web: https://console.s3.example.com
# 3. Access Key: El valor de ADMIN_EMAIL
# 4. Secret Key: El valor de ADMIN_PASSWORD
# 5. Compatible con AWS S3 API
# 6. Crear buckets desde la consola web
# 7. ADMIN_PASSWORD debe tener mínimo 8 caracteres

# Ejemplo de uso con mc (MinIO Client):
# mc alias set myminio https://s3.example.com admin CHANGE_ME_RANDOM_PASSWORD
# mc mb myminio/mybucket
# mc cp file.txt myminio/mybucket/

# Conexión desde otras apps:
# S3_ENDPOINT=https://s3.example.com
# S3_ACCESS_KEY=admin
# S3_SECRET_KEY=CHANGE_ME_RANDOM_PASSWORD
# S3_BUCKET=mybucket
# S3_REGION=us-east-1