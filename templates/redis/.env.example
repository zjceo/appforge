# Redis Configuration
# Generado automáticamente por AppForge

# Dominio (Redis Commander Web UI)
DOMAIN=redis.example.com
APP_NAME=redis_1
SERVICE_NUMBER=1

# Base de datos Redis (Redis usa números 0-15)
DB_NAME=0
DB_USER=default
DB_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# Credenciales de administrador (para Redis Commander)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# API Key (no aplica en Redis)
API_KEY=CHANGE_ME_RANDOM_KEY

# Puerto de Redis (expuesto externamente)
DB_PORT=6379

# Configuración de Redis
REDIS_MAX_MEMORY=256mb
REDIS_MAX_MEMORY_POLICY=allkeys-lru

# Notas de instalación:
# 1. Redis Commander Web UI: https://redis.example.com
# 2. Usuario Web UI: el configurado en ADMIN_EMAIL
# 3. Password Web UI: el configurado en ADMIN_PASSWORD
# 4. Conexión Redis: redis://:PASSWORD@IP:6379
# 5. Password Redis: el configurado en DB_PASSWORD

# Conectar con redis-cli:
# docker exec -it redis_1 redis-cli -a PASSWORD

# Connection string para aplicaciones:
# redis://:PASSWORD@redis_1:6379
# redis://:PASSWORD@redis_1:6379/0  (especificar DB)

# Comandos útiles:
# PING                 # Verificar conexión
# KEYS *               # Ver todas las keys
# GET key              # Obtener valor
# SET key value        # Establecer valor
# DEL key              # Eliminar key
# INFO                 # Info del servidor
# FLUSHALL             # Borrar todo (¡cuidado!)

# Políticas de eviction:
# - allkeys-lru: Elimina las keys menos usadas
# - allkeys-lfu: Elimina las keys menos frecuentemente usadas
# - allkeys-random: Elimina keys aleatorias
# - volatile-lru: Elimina keys con TTL menos usadas