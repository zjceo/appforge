# MongoDB Configuration
# Generado automáticamente por AppForge

# Dominio (Mongo Express Web UI)
DOMAIN=mongo.example.com
APP_NAME=mongodb_1
SERVICE_NUMBER=1

# Base de datos MongoDB
DB_NAME=admin
DB_USER=admin
DB_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# Credenciales de administrador (para Mongo Express)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGE_ME_RANDOM_PASSWORD

# API Key (no aplica en MongoDB)
API_KEY=CHANGE_ME_RANDOM_KEY

# Puerto de MongoDB (expuesto externamente)
DB_PORT=27017

# Notas de instalación:
# 1. Mongo Express Web UI: https://mongo.example.com
# 2. Usuario Web UI: el configurado en ADMIN_EMAIL
# 3. Password Web UI: el configurado en ADMIN_PASSWORD
# 4. Conexión MongoDB: mongodb://admin:PASSWORD@IP:27017/admin?authSource=admin
# 5. Usuario MongoDB: el configurado en DB_USER
# 6. Password MongoDB: el configurado en DB_PASSWORD

# Conectar con mongosh:
# docker exec -it mongodb_1 mongosh -u admin -p PASSWORD --authenticationDatabase admin

# Connection string para aplicaciones:
# mongodb://admin:PASSWORD@mongodb_1:27017/mydb?authSource=admin

# Crear usuario para una DB específica:
# use mydb
# db.createUser({
#   user: "myuser",
#   pwd: "mypassword",
#   roles: [{role: "readWrite", db: "mydb"}]
# })